<!--components/w-good-item/w-goods-item.wxml-->
<view class="goods-item" bind:tap="itemClick">
      <image src="{{item.show.img || item.img || item.image}}" mode="widthFix" class="image"/>
    <view class="desc-info">
      <view class="title">{{item.title}}</view>
      <view class="info">
        <text class="price">￥{{item.price}}</text>
          <image src="../../assets/common/favor.png" class="icon"/> 
        <text class="cfav">{{item.cfav}}</text>
      </view>
    </view>
</view>
